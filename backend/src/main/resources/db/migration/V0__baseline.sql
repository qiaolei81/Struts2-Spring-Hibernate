-- Flyway baseline migration placeholder.
-- Real schema DDL will be added in V1__create_initial_schema.sql by the database agent (t5).
-- This file establishes the Flyway baseline so the tool can track subsequent migrations.
SELECT 1;
